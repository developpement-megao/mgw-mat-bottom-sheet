import { LinkBuildPipe } from './link-build.pipe';

describe('LinkBuildPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkBuildPipe();
    expect(pipe).toBeTruthy();
  });
});
